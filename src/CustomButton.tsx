import { Button, Heading } from "@radix-ui/themes";

const CustomButton = () => {
  return (
    <Button size={"4"} color="gray" highContrast>
      <Heading>Get your Diet plan now</Heading>
    </Button>
  );
};

export default CustomButton;
